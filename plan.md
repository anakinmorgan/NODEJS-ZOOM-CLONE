# Plan Of Action

- Initialize our NODE JS Project
- Initialize our first view
- Create a room id
- Add the ablility to view our own video
- Add ablility to allow other to stream their video
- Add styling
- Add the ablilty to create messages
- Add mute button
- Add stop Video button


npm init initalize our nodejs app(back end)
npm install express - how we build our API
npm install ejs - help to get variables from back end to font end
npm install uuid - this will allow unique IDs for each person enter a room
npm install socket.io - this is a live service to have for live stream and chat
npm install peer - this is peer to peerJS
npm install -g heroku - his installs global to heroku so i can deploy the app